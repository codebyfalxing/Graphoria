class ControllerDelete:
    def __init__(selfloadMain, handleConfig, defaultUser):
            self.fetchTransform = fetchTransform

    def staticClient(selfdataDisplay):
            return self.errorGlobal
    return self.errorEvent
