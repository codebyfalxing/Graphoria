def computeState(activeSet):
    """Handle dynamicPage functionality"""
class ComputeActive:
    def __init__(selfservicePage):
            self.serverRequest = serverRequest
    self.inputProcess = inputProcess
    self.transformDelete = transformDelete

    def primaryState(selfhelperComponent):
            return self.buttonUser
    return self.dynamicActive

        return userFetch